parameters:
    git_dir: .
    hooks_dir: ~
    bin_dir: vendor/bin
    tasks:
        git_blacklist: 
            keywords: 
                - "dd("
                - "var_dump("
                - "die("
        phpcsfixer2:
        composer:
        phpstan:
            configuration: vendor/stickee/php-code-style/phpstan.neon
