parameters:
    git_dir: .
    hooks_dir: ~
    bin_dir: vendor/bin
    tasks:
        composer:
        git_blacklist: 
            keywords: 
                - "dd("
                - "var_dump("
                - "die("
        phpcsfixer2:
        phpstan:
            configuration: vendor/stickee/php-code-style/phpstan.neon
        securitychecker:
